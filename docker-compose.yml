services:
  mongo:
    image: mongo:7
    container_name: xtract-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
  azurite:
    image: mcr.microsoft.com/azure-storage/azurite:3.34.0
    container_name: xtract-azurite
    command: azurite --skipApiVersionCheck --blobHost 0.0.0.0 --queueHost 0.0.0.0 --tableHost 0.0.0.0
    ports:
      - "10000:10000"
      - "10001:10001"
      - "10002:10002"
    volumes:
      - azurite-data:/data

volumes:
  mongo-data:
  azurite-data:
